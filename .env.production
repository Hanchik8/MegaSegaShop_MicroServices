# Production environment template for docker-compose
# Fill with real values and keep this file out of version control if needed.

# JWT secret for auth-service/api-gateway (min 32 chars; 64+ recommended)
SECURITY_JWT_SECRET=REPLACE_WITH_LONG_RANDOM_SECRET

# JWT expiration in minutes (optional; default: 60)
SECURITY_JWT_EXPIRATION_MINUTES=60

# PostgreSQL credentials (shared across services)
DB_USERNAME=megasegashop_prod
DB_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
